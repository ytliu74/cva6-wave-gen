#include <stdint.h>
#include <stdio.h>

int main(int argc, char* arg[]) {

        printf("%d: Hello World !", 0);

        int a = 0;
        for (int i = 0; i < 5; i++)
        {
                a += i;
        }
        return 0;
}

